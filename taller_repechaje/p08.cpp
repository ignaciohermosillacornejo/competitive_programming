#include <bits/stdc++.h>

std::queue < std::pair< int, int > > schedule;

int main()
{
    int n;
    std::cin >> n;
    std::pair <int, int> temp;
    for (int i = 0; i < n; i++){
        int a; int p;
        std::cin >> a >> p;
        temp = std::make_pair (a, p);
        
    }

    return 0;
}